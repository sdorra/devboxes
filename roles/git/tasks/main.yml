- name: Install git and related tools (MacOSX)
  homebrew:
    name:
    - git
    - git-crypt
    - gh
  when: ansible_distribution == "MacOSX"

- name: Install git and related tools (Ubuntu)
  become: True
  include: ubuntu.yml
  when: ansible_distribution == "Ubuntu"
